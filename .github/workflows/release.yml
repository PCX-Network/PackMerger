name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build shadow JAR
        run: ./gradlew shadowJar

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: PackMerger ${{ github.ref_name }} (test build)
          files: build/libs/PackMerger-${{ steps.version.outputs.version }}.jar
          generate_release_notes: true
          prerelease: true
          body: |
            :warning: **Test build — not a stable release.**

            This build is being shared with a specific user to validate a fix
            (QualityArmory resource-pack merge compatibility). Please wait for a
            later non-prerelease version before using in production.
